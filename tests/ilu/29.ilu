@include "std/io.ilu"
@include "std/string.ilu"

def main in
    argv argc 8 * 8 - +  # point at last value in argv
    read64 cast(ptr)
    dup strlen           # get length of value
    swp println
end
