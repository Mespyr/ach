# comments

# print 0-10 using syscalls
0 while dup 11 < do
    # push mem onto stack
    mem
    # get ascii value to print out
    swp dup 48 +
    # write to memory
    rot swp store
    # perform write syscall
    1 1 mem 1 syscall3
    # increment counter
    1 +
end
