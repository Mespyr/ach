include "std/core.ach"

def main in
	1 while dup 101 < do
		if dup 15 modulo 0 = do
			"Fizzbuzz\n" puts
		else if dup 3 modulo 0 = do
			"Fizz\n" puts
		else if dup 5 modulo 0 = do
			"Buzz\n" puts
		else
			dup putu
			"\n" puts
		end end end
		1 +
	end pop
end
