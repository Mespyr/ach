include "std/core.ach"

def main in
	1 while dup 101 < do
		dup 15 modulo 0 = if
			"Fizzbuzz\n" puts
		else dup 3 modulo 0 = if
			"Fizz\n" puts
		else dup 5 modulo 0 = if
			"Buzz\n" puts
		else
			dup putu
			"\n" puts
		end end end
		1 +
	end pop
end
