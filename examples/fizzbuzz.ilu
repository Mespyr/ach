@include "std/io.ilu"
@include "std/math.ilu"

def main
    1 while dup 101 < do
        dup 15 modulo 0 = if
            "Fizzbuzz\n" puts
        else dup 3 modulo 0 = if
            "Fizz\n" puts
        else dup 5 modulo 0 = if
            "Buzz\n" puts
        else
            dup dump
        end end end
        1 +
    end pop
end
