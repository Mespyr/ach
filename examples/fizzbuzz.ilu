@include "std/io.ilu"
@include "std/math.ilu"

def main in
    1 while dup 101 < do
        dup 15 modulo 0 = if
            "Fizzbuzz" println
        else dup 3 modulo 0 = if
            "Fizz" println
        else dup 5 modulo 0 = if
            "Buzz" println
        else
            dup dump
        end end end
        1 +
    end pop
end
