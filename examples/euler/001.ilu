@include "std/math.ilu"

def main in
    0 3 while dup 1000 < do
        dup 3 modulo 0 =
        over 5 modulo 0 =
        orb if
            swp over + swp
        end

        1 +
    end pop
    dump
end
